# Nuxt
.nuxt
# 由 @nuxt/test-utils 等模組在本機跑 test / typecheck 時自動寫入，內容是機器產生的
# setups 記錄，各人不同也不需共用
.nuxtrc
.nitro
.cache
.output
.vercel
.nuxtrc
dist

# NuxtHub / Wrangler
.data
.wrangler

# Dependencies
node_modules

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# Tests
coverage
test-results
playwright-report
blob-report
.playwright

# Agent 在 CI 上下載的 Epic 截圖（workflow 產生，不進版控）
.agent-images

# workflow 取出的 issue 內文與過濾後的人類 review（只在 CI 上產生，不進版控）
.agent-issue.md
.agent-review.md

# Editor / OS
.DS_Store
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
