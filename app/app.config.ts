export default defineAppConfig({
  ui: {
    colors: {
      // Epic #1 screen-1 的主色取樣：選中的「首頁」tab、「+ 新增」按鈕、水質數值
      // 三處都是 #2dd4bf ＝ teal-400。骨架原本的 cyan-400（#22d3ee）偏藍，與截圖不符。
      primary: 'teal',
      neutral: 'slate',
    },
  },
})
